## Getting Started

##  Weather API in the project ##:  The rules we’ll follow (Documentation specified with uses Typescript/React/Nextjs/Better-Auth)

Domain logic → lib/weather/*
Reusable UI components → app/components/*
Route composition → app/weather/*
